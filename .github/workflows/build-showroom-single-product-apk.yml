name: Build Showroom Single Product APK

on:
  push:
    branches:
      - apk/single-product-demo-20260828
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.9'

      - name: Build debug APK
        run: gradle -p showroom-single-product assembleDebug --stacktrace

      - name: Rename APK
        run: cp showroom-single-product/app/build/outputs/apk/debug/app-debug.apk single-product-demo.apk

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: single-product-demo-apk
          path: single-product-demo.apk
          if-no-files-found: error
